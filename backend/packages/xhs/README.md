# XHS

Xiaohongshu crawling package.

- load and validate the fixed watchlist config
- open a logged-in Playwright browser for profile pages
- resolve first-screen note candidates without pagination
- fetch note detail text anonymously over HTTP
- persist raw note JSONL, monitor state, and debug snapshots
